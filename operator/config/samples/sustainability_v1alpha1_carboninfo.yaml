apiVersion: sustainability.susk8s/v1alpha1
kind: CarbonInfo
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: carboninfo-sample
spec:
  # TODO(user): Add fields here
