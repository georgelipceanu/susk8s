---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: carboninfoes.sustainability.susk8s
spec:
  group: sustainability.susk8s
  names:
    kind: CarbonInfo
    listKind: CarbonInfoList
    plural: carboninfoes
    shortNames:
    - carbon
    singular: carboninfo
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.currentIntensity
      name: Intensity
      type: integer
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .status.lastUpdated
      name: Last_Update
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          CarbonInfo is the Schema for the carboninfoes API.
          It represents regional or node-group carbon intensity as a live signal for the cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              CarbonInfoSpec defines the desired state of CarbonInfo.
              It configures how and where the system should fetch carbon intensity data.
            properties:
              overrideIntensity:
                format: int32
                type: integer
              pollSeconds:
                default: 300
                description: PollSeconds defines the refresh cadence for fetching
                  live intensity data.
                format: int32
                type: integer
              provider:
                description: Provider is the data source.
                type: string
              region:
                description: Region is the provider-specific geographic key for the
                  grid data.
                type: string
              staticIntensity:
                description: StaticIntensity provides a fallback or fixed gCO2/kWh
                  value for static mode.
                format: int32
                type: integer
            required:
            - provider
            - region
            type: object
          status:
            description: CarbonInfoStatus defines the observed state of CarbonInfo.
            properties:
              conditions:
                description: Conditions represent the current state of the carbon
                  signal (e.g., Ready, Error).
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentIntensity:
                description: CurrentIntensity is the latest fetched grid carbon intensity
                  in gCO2/kWh.
                format: int32
                type: integer
              lastUpdated:
                description: LastUpdated is the timestamp of the last successful data
                  retrieval from the provider.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
